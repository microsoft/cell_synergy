# Thymus

This directory contains SLURM batch scripts for running computational tasks.

## Main Script

The main script is `thymus_train_comm_cfg1.sbatch` which runs the primary task for this directory.

### Configuration cfg2

- `thymus_train_comm_cfg2.sbatch` - Runs with configuration cfg2 - For thymus dataset
- `thymus_train_comm_cfg2_4gpu.sbatch` - Runs with configuration cfg2 - For thymus dataset

### Configuration cfg23

- `thymus_train_comm_cfg23.sbatch` - Runs with configuration cfg23 - For thymus dataset

### Configuration cfg24

- `thymus_train_comm_cfg24.sbatch` - Runs with configuration cfg24 - For thymus dataset

### Configuration cfg25

- `thymus_train_comm_cfg25.sbatch` - Runs with configuration cfg25 - For thymus dataset

### Configuration cfg3

- `thymus_train_comm_cfg3.sbatch` - Runs with configuration cfg3 - For thymus dataset
- `thymus_train_comm_cfg3_4gpu.sbatch` - Runs with configuration cfg3 - For thymus dataset

### Configuration cfg4

- `thymus_train_comm_cfg4.sbatch` - Runs with configuration cfg4 - For thymus dataset
- `thymus_train_comm_cfg4_4gpu.sbatch` - Runs with configuration cfg4 - For thymus dataset

### Configuration cfg5

- `thymus_train_comm_cfg5.sbatch` - Runs with configuration cfg5 - For thymus dataset
- `thymus_train_comm_cfg5_4gpu.sbatch` - Runs with configuration cfg5 - For thymus dataset

### Configuration cfg6

- `thymus_train_comm_cfg6_4gpu.sbatch` - Runs with configuration cfg6 - For thymus dataset


## Usage

```bash
sbatch thymus_train_comm_cfg1.sbatch
```
