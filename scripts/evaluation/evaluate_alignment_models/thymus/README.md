# Thymus

This directory contains SLURM batch scripts for running computational tasks.

## Main Script

The main script is `eval_thymus_dcca_v1.sbatch` which runs the primary task for this directory.

### Configuration cfg1

- `eval_thymus_comm_cfg1_job32029277.sbatch` - Runs with configuration cfg1 - For thymus dataset - Uses specific checkpoint from job job32029277

### Configuration cfg2

- `eval_thymus_comm_cfg2_job32029278.sbatch` - Runs with configuration cfg2 - For thymus dataset - Uses specific checkpoint from job job32029278

### Configuration cfg23

- `eval_thymus_comm_cfg23_job32162083.sbatch` - Runs with configuration cfg23 - For thymus dataset - Uses specific checkpoint from job job32162083

### Configuration cfg25

- `eval_thymus_comm_cfg25_job32162093.sbatch` - Runs with configuration cfg25 - For thymus dataset - Uses specific checkpoint from job job32162093

### Configuration cfg3

- `eval_thymus_comm_cfg3_job32029279.sbatch` - Runs with configuration cfg3 - For thymus dataset - Uses specific checkpoint from job job32029279

### Configuration cfg4

- `eval_thymus_comm_cfg4_job32029280.sbatch` - Runs with configuration cfg4 - For thymus dataset - Uses specific checkpoint from job job32029280

### Configuration cfg5

- `eval_thymus_comm_cfg5_job32029281.sbatch` - Runs with configuration cfg5 - For thymus dataset - Uses specific checkpoint from job job32029281

### Configuration v2

- `eval_thymus_dcca_v2.sbatch` - Runs with configuration v2 - For thymus dataset

### Configuration v3

- `eval_thymus_dcca_v3.sbatch` - Runs with configuration v3 - For thymus dataset


## Usage

```bash
sbatch eval_thymus_dcca_v1.sbatch
```
