# Breast

This directory contains SLURM batch scripts for running computational tasks.

## Main Script

The main script is `eval_breast_comm_cfg1_job32029272.sbatch` which runs the primary task for this directory.

### Configuration cfg2

- `eval_breast_comm_cfg2_job32029273.sbatch` - Runs with configuration cfg2 - For breast dataset - Uses specific checkpoint from job job32029273

### Configuration cfg23

- `eval_breast_comm_cfg23_job32162094.sbatch` - Runs with configuration cfg23 - For breast dataset - Uses specific checkpoint from job job32162094

### Configuration cfg24

- `eval_breast_comm_cfg24_job32162095.sbatch` - Runs with configuration cfg24 - For breast dataset - Uses specific checkpoint from job job32162095

### Configuration cfg25

- `eval_breast_comm_cfg25_job32162096.sbatch` - Runs with configuration cfg25 - For breast dataset - Uses specific checkpoint from job job32162096

### Configuration cfg3

- `eval_breast_comm_cfg3_job32029274.sbatch` - Runs with configuration cfg3 - For breast dataset - Uses specific checkpoint from job job32029274

### Configuration cfg4

- `eval_breast_comm_cfg4_job32029275.sbatch` - Runs with configuration cfg4 - For breast dataset - Uses specific checkpoint from job job32029275

### Configuration cfg5

- `eval_breast_comm_cfg5_job32029276.sbatch` - Runs with configuration cfg5 - For breast dataset - Uses specific checkpoint from job job32029276


## Usage

```bash
sbatch eval_breast_comm_cfg1_job32029272.sbatch
```
